# Cloudflare Workers configuration for the SkyClear site.
# This is a static-asset-only deployment — no server code, no Worker handler.
# Cloudflare serves the files in this directory directly via its CDN.

name = "skyclear-site"
compatibility_date = "2026-05-15"

[assets]
directory = "."

[observability]
enabled = true
